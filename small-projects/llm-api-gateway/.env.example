# Provider API keys
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
# Local model endpoint (e.g., Ollama, vLLM)
LOCAL_MODEL_URL=http://localhost:11434

# Routing strategy: cost, latency, quality, round-robin
ROUTING_STRATEGY=cost
# Cache TTL in seconds (0 to disable)
CACHE_TTL=3600
